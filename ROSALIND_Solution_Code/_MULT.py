##     Multiple Alignment 

from utility import readFastaFileList

def mult(seqList):
    size = len(seqList)
    lenList = []
    for i in range(size):
        lenList[i] == len(seqList[i])
    
    arr = []
    for i in range()

